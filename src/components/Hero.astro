---
import type { Locale } from '../lib/i18n';

export interface Props {
  lang?: Locale;
}

const { lang = 'fr' } = Astro.props;

const t = {
  fr: {
    badge: '100% Gratuit · Sans Inscription',
    line1: 'Des calculateurs simples',
    line2: 'pour',
    line2Accent: 'mieux gérer',
    line3: 'votre quotidien',
    subtitle:
      "Rapide, gratuit, sans inscription. Calculez localement pour votre tranquillité d'esprit avec des outils professionnels et précis.",
    discoverTools: 'Découvrir les outils',
    free: 'Gratuit',
    noSignup: 'Sans inscription',
    local: 'Calcul local',
  },
  en: {
    badge: '100% Free · No Signup',
    line1: 'Simple calculators',
    line2: 'to',
    line2Accent: 'better manage',
    line3: 'your daily finances',
    subtitle:
      'Fast, free, no signup required. Run local calculations for peace of mind with clear and practical tools.',
    discoverTools: 'Explore tools',
    free: 'Free',
    noSignup: 'No signup',
    local: 'Local calculation',
  },
}[lang];

const calculatorsPath = lang === 'en' ? '/en/calculators/' : '/fr/calculateurs/';
---

<section class="hero">
  <div class="hero-content">
    <div class="hero-badge">
      <span>✨</span>
      <span>{t.badge}</span>
    </div>

    <h1>
      {t.line1}<br />
      {t.line2} <span class="gradient-text">{t.line2Accent}</span><br />
      {t.line3}
    </h1>

    <p class="hero-subtitle">
      {t.subtitle}
    </p>

    <div class="hero-ctas">
      <a href={calculatorsPath} class="btn btn-primary">
        {t.discoverTools}
        <svg width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <path d="M5 12h14m-7-7l7 7-7 7" />
        </svg>
      </a>
    </div>

    <div class="hero-features">
      <div class="feature-badge">
        <span class="feature-icon">⚡</span>
        <span>{t.free}</span>
      </div>
      <div class="feature-badge">
        <span class="feature-icon">🔒</span>
        <span>{t.noSignup}</span>
      </div>
      <div class="feature-badge">
        <span class="feature-icon">💻</span>
        <span>{t.local}</span>
      </div>
    </div>
  </div>
</section>
