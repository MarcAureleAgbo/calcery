---
export interface Props {
  label?: string;
  placeholder?: string;
}

const {
  label = 'Publicité',
  placeholder = 'Espace publicitaire Google AdSense',
} = Astro.props;
---

<div class="ad-space">
  <div class="ad-label">{label}</div>
  <div class="ad-placeholder">{placeholder}</div>
</div>
